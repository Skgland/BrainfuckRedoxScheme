name: Rust

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Add target
      run: rustup target add x86_64-unknown-redox
    - name: Build
      run: cargo build --target x86_64-unknown-redox --verbose
    - name: Run tests
      run: cargo test --verbose
